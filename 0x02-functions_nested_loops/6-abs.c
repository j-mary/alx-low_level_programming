#include "main.h"

/**
 *_abs - Check Description
 * @i: The integer to check
 * Description: It returns the absolute value of an integer
 * Return:  absolute value of integer i
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
